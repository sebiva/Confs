# Set prefix key to Ctrl-h
#unbind-key C-b
#set-option -g prefix C-h

#fix colors
set -g default-terminal "xterm-256color"
 
# send the prefix to client inside window
#bind-key C-h send-prefix

# start numbering at 1
set -g base-index 1

# set source file
#bind r source-file /home/or3x/.tmux.conf

# session set up
new -s tmuxx -n Home
neww -d -n Algorithms -c /home/or3x/Documents/Chalmers-Kurser/Advanced_Algorithms
neww -d -n Models -c /home/or3x/Documents/Chalmers-Kurser/Models_of_Computation

neww -n Telegram -c /home/or3x/
send-keys -t tmuxx.0 telegram Enter
neww -n Jolla -c /home/or3x/
send-keys -t tmuxx.0 ssh Space nemo@192.168.0.12

selectw -t 1 

bind '"' split-window -h -c "#{pane_current_path}"
bind % split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"



## Kandidat -- old

# new -s tmuxx -n Home
# neww -d -n Kandidat -c /home/or3x/Documents/Chalmers-Kurser/Kandidat/
# neww -n Telegram -c /home/or3x/
# send-keys -t tmuxx.0 telegram Enter

# neww -d -n Paxos -c /home/or3x/Documents/Chalmers-Kurser/Kandidat/paxos
# neww -d -n Gui -c /home/or3x/Documents/Chalmers-Kurser/Kandidat/gulliverbachelor2014/software/gui_client/
# neww -d -n MTS -c /home/or3x/Documents/Chalmers-Kurser/MTS/
# neww -n Jolla -c /home/or3x/
# send-keys -t tmuxx.0 ssh Space nemo@192.168.0.12




## Tmux Plugin manager

# List of plugins
#
# Resurrect for restoring sessions
#
# Supports `github_username/repo` or full git URLs
set -g @tpm_plugins "              \
  tmux-plugins/tpm                 \
  tmux-plugins/tmux-sensible       \
"
#   tmux-plugins/tmux-resurrect      \

# Other examples:
# github_username/plugin_name    \
# git@github.com/user/plugin     \
# git@bitbucket.com/user/plugin  \

# initializes TMUX plugin manager
run-shell ~/.tmux/plugins/tpm/tpm
#run-shell ~/.tmux/plugins/tmux-resurrect/resurrect.tmux




# Testing

set-window-option -g mode-keys vi

# Smart pane switching with awareness of vim splits
bind -n C-h run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys C-h) || tmux select-pane -L"
bind -n C-j run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys C-j) || tmux select-pane -D"
bind -n C-k run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys C-k) || tmux select-pane -U"
bind -n C-l run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys C-l) || tmux select-pane -R"
bind -n C-\ run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys 'C-\\') || tmux select-pane -l"
